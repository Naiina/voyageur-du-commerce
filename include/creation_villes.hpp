#ifndef CREATION_VILLE.H
#define CREATION_VILLE.H

void creation_graphe(uint n, string type){
    cout << endl << "creation " << n << " villes en " << type << endl;
    Ville::nbVille = 0; ///initialisation à 0 du nbre de villes
    vector<Ville> liste_villes;
    if(type == "ROND"){
        float dd =
    }
    for(uint i = 0; i < ,; i++){

    }
}

#endif // CREATION_VILLE
