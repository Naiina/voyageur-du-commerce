#ifndef CROSS_OVER_H
#define CROSS_OVER_H
#include "Chemin.hpp"
#include <vector>
#include <cstdlib>
#include <iostream>
using namespace std;

vector<Chemin> cross_over(const Graphe& graphe, const Chemin & I, const Chemin & J);

#endif
